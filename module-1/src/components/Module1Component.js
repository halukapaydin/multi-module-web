import React from 'react';
import ReactDOM from 'react-dom';
import {CommonComponent} from "module-common";

export default ()=>{
	return <div>
		<div>Module 1 component</div>
		<div><CommonComponent /></div>
	</div>
}